# RouterOS virtual routers boot more slowly than Linux endpoints. Keep trying
# frequently so a fresh lab deployment does not wait on dhclient's long default
# retry interval after its first discovery window expires.
timeout 20;
retry 5;
initial-interval 1;
